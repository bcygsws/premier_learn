/**
 * @Desc；跨域
 * @Author: Bao Chengyi
 * 跨域是浏览器基于同源策略的一种安全手段
 *
 * 同源策略
 * 定义：是浏览器最核心也最基本的安全功能
 * protocol + host + port
 * 协议+主机+端口
 * 三者有一个不同就不同源
 *
 * 补充：对于用抓包工具抓到的请求，浏览器会忽略同源策略；同时，使用postman请求数据，也会跳过这一限制；
 * 原因，还是这个同源策略是基于浏览器的
 *
 *
 *
 **/

